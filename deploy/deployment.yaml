---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: image-existence-exporter
  namespace: kube-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-existence-exporter
  namespace: kube-system
spec:
  selector:
    matchLabels:
      app: image-existence-exporter
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: image-existence-exporter
    spec:
      containers:
        - args:
            - --bind-address=:8080
            - --ignored-images="nginx:1.11"
          ports:
            - containerPort: 8080
              name: http-metrics
          image: flant/k8s-image-existence-exporter:v0.2
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
              scheme: HTTPS
          name: image-existence-exporter
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
              scheme: HTTP
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
      serviceAccountName: image-existence-exporter
